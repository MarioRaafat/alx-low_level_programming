#include <stdio.h>

int main(void)
{
	int n; /*declaration n*/

	if (n > 0)
	printf("%i is positive\n", n);
	else if (n < 0)
	printf("%i is negative\n", n);
	else
	printf("%d is zero\n", n);
	return (0);
}
